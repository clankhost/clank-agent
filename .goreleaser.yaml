version: 2
project_name: clank-agent

builds:
  - main: .
    binary: clank-agent
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/clankhost/clank-agent/cmd.Version={{.Version}}
      - -X github.com/clankhost/clank-agent/cmd.Commit={{.ShortCommit}}
      - -X github.com/clankhost/clank-agent/cmd.Date={{.Date}}

archives:
  - format: tar.gz
    name_template: "clank-agent_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
